# Provider keys for the dispatch layer (used by `tack dispatch`).
# Tack reads whichever key corresponds to the model you assign to a tier.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GOOGLE_GENERATIVE_AI_API_KEY=
